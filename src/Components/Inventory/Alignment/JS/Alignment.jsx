import React from "react";
import classes from "../CSS/Alignment.module.css";

const Alignment = ({ children }) => {
  return <div className={classes.Alignment}>{children}</div>;
};

export default Alignment;
